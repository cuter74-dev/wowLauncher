name: shared
description: Shared models, API DTOs and utilities for Remote Launcher.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

# Pure Dart package (no Flutter dependency) so it can be reused by both
# the desktop agent and the mobile app. No runtime dependencies.
dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
